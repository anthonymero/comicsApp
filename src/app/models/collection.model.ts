export interface ICollection {
    name: string;
    editor: string;
    volumeCount: number;
    // Todo enum (over , in progress)
    state: string;
    // Todo enum (adventure, fantastic ...)
    style: string;
}
