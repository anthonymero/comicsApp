export interface IBook {
  uid?: string;
  userId?: string;
  title: string;
  // Tome
  volume: string;
  year?: string;
  // Scenario
  scenario?: string;
  // Dessin
  drawing?: string;
  // Couleur
  colors?: string;
  cover?: string;

}
